spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/blog_db
    username: jim
    password: password

  flyway:
    user: jim
    password: password
    url: jdbc:mariadb://localhost:3306/blog_db
    locations: classpath:db.migration

server:
  port: 8080