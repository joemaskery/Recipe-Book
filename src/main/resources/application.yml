spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/recipe-book-db
    username: jim
    password: password

  flyway:
    user: jim
    password: password
    url: jdbc:mariadb://localhost:3306/recipe-book-db
    locations: classpath:db.migration

server:
  port: 8080